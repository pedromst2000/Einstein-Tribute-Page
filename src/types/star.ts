/**
 * Represents a single star in the starfield
 */
export type Star = {
  id: number;
  x: number;
  y: number;
  size: number;
  animationDuration: string;
  animationDelay: string;
  color: string;
};
